// Функция ymaps.ready() будет вызвана, когда
    // загрузятся все компоненты API, а также когда будет готово DOM-дерево.
    ymaps.ready(init);
 
    function init() {
        // Создание карты.
        // https://tech.yandex.ru/maps/doc/jsapi/2.1/dg/concepts/map-docpage/
        var myMap = new ymaps.Map("map", {
            // Координаты центра карты.
            // Порядок по умолчнию: «широта, долгота».
            center: [55.7595, 37.638],
            // Уровень масштабирования. Допустимые значения:
            // от 0 (весь мир) до 19.
            zoom: 18,
            // Элементы управления
            // https://tech.yandex.ru/maps/doc/jsapi/2.1/dg/concepts/controls/standard-docpage/
            controls: []
        });
         myMap.behaviors.disable('scrollZoom');
        // Добавление метки
        // https://tech.yandex.ru/maps/doc/jsapi/2.1/ref/reference/Placemark-docpage/
        var myPlacemark = new ymaps.Placemark([55.76, 37.64], {
              hintContent: 'Метка 1',
        }, {
         // Опции.
 
        // Необходимо указать данный тип макета.
        iconLayout: 'default#image',
 
        // Своё изображение иконки метки.
        iconImageHref: '../img/Vector.png',
        // Размеры метки.
        iconImageSize: [60, 79],
        // Смещение левого верхнего угла иконки относительно
        // её "ножки" (точки привязки).
        iconImageOffset: [-16, -16],
        });
    
        // После того как метка была создана, добавляем её на карту.
        myMap.geoObjects.add(myPlacemark);
 
    }

$('.tel_open .show').on('click', function(){
    $(this).closest('.tel_open').children('span').toggle();
    $(this).closest('.tel_open').children('.tel').toggle();
    $(this).closest('.tel_open').children('.show').css('display','none');
    // if(($(this).closest('.tel_open').children('.show').html()) == 'показать'){
    //     $(this).closest('.tel_open').children('.show').html('скрыть');
    //     $(this).closest('.tel_open').children('.show').css('top','0');
    // }else{
    //     $(this).closest('.tel_open').children('.show').html('показать');
    //     $(this).closest('.tel_open').children('.show').css('top','-11px');
    // }

});

$('.details .knowmore').on('click', function(){
    if ($('.details').hasClass('active')){
        $('.details').removeClass('active');
    }else{
        $('.details').addClass('active');
    }
    
});